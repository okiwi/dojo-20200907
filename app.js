const calc = {
    add: (firstNumber, secondNumber) => {
        var bothNumbers = firstNumber + secondNumber
        if (bothNumbers === "IIII") 
            return "IV"
        
        return bothNumbers
    }
}
module.exports = calc